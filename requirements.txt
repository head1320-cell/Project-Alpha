# ═══════════════════════════════════════════════════════════════════════════════
# Project Alpha — Python 의존성
# ═══════════════════════════════════════════════════════════════════════════════
# 핵심(CORE)만 설치해도 API + 5개 모듈 + 백테스트 + 자동매매가 모두 작동합니다.
# torch는 LSTM 변동성 예측 1개 기능에만 쓰이는 선택적(OPTIONAL) 의존성입니다.
#
# 설치:
#   pip install -r requirements.txt              # 핵심만 (권장, 빠름)
#   pip install -r requirements.txt torch>=2.0.0 # LSTM 기능까지
# ═══════════════════════════════════════════════════════════════════════════════

# ─── 웹 프레임워크 / 서버 ───
fastapi==0.111.0
uvicorn==0.30.1
pydantic>=2.0.0
httpx==0.27.0

# ─── 데이터 / 수치 연산 ───
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.0

# ─── 정량 모델 (VaR / GARCH / 파생) ───
arch>=6.0.0
statsmodels>=0.14.0
QuantLib>=1.30
networkx>=3.0

# ─── 데이터베이스 ───
sqlalchemy==2.0.30
aiosqlite>=0.19.0
psycopg2-binary>=2.9.0
asyncpg>=0.29.0

# ─── 인증 / 보안 ───
bcrypt>=4.0.0
PyJWT>=2.8.0
python-multipart>=0.0.9

# ─── 외부 데이터 소스 ───
requests==2.32.3
yfinance==0.2.40
websocket-client==1.8.0

# ─── 유틸리티 ───
python-dotenv==1.0.1
pyyaml>=6.0
apscheduler>=3.10.0
xlsxwriter==3.2.0
reportlab>=4.0.0
matplotlib>=3.8.0
anthropic>=0.39.0

# ─── 테스트 ───
pytest==8.2.2

# ═══════════════════════════════════════════════════════════════════════════════
# OPTIONAL — 아래는 수동 설치 (특정 기능 전용)
# ═══════════════════════════════════════════════════════════════════════════════
# torch>=2.0.0      # LSTM 변동성 예측 (/ai-vol-compare, /lstm-forecast)
#                   # 미설치 시 해당 2개 엔드포인트만 503 반환, 나머지는 정상

# 분봉 적재 권장 (없으면 csv.gz 폴백으로 동작): 
# pyarrow>=14.0.0
